# Auto detect text files and normalize line endings to LF
* text=auto eol=lf

# Windows scripts can use CRLF if needed
*.bat text eol=crlf

# Binary files (no line ending conversion)
*.png binary
*.jpg binary
*.pdf binary
